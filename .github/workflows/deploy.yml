name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGISTRY: docker.io
  IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/nxt-1-astro

jobs:
  check-secrets:
    name: Check Required Secrets
    runs-on: ubuntu-latest
    outputs:
      docker-configured: ${{ steps.check.outputs.docker-configured }}
      server-configured: ${{ steps.check.outputs.server-configured }}
    steps:
      - name: Check if secrets are configured
        id: check
        run: |
          if [[ -n "${{ secrets.DOCKERHUB_USERNAME }}" && -n "${{ secrets.DOCKERHUB_TOKEN }}" ]]; then
            echo "docker-configured=true" >> $GITHUB_OUTPUT
            echo "✅ Docker Hub credentials configured"
          else
            echo "docker-configured=false" >> $GITHUB_OUTPUT
            echo "⚠️ Docker Hub credentials not configured - skipping Docker build"
          fi
          
          if [[ -n "${{ secrets.SERVER_HOST }}" && -n "${{ secrets.SERVER_USERNAME }}" ]]; then
            echo "server-configured=true" >> $GITHUB_OUTPUT
            echo "✅ Server credentials configured"
          else
            echo "server-configured=false" >> $GITHUB_OUTPUT
            echo "⚠️ Server credentials not configured - skipping deployment"
          fi

  build-and-push:
    name: Build and Push Docker Image
    needs: check-secrets
    if: needs.check-secrets.outputs.docker-configured == 'true'
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to Server
    needs: [check-secrets, build-and-push]
    if: needs.check-secrets.outputs.server-configured == 'true' && needs.build-and-push.result == 'success'
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script: |
            # Navigate to deployment directory
            cd ${{ secrets.SERVER_PATH || '/opt/nxt-1-astro' }}
            
            # Pull latest image
            echo "🔄 Pulling latest image..."
            docker-compose pull
            
            # Deploy with zero downtime
            echo "🚀 Deploying new version..."
            docker-compose up -d --remove-orphans
            
            # Wait for container to be healthy
            echo "⏳ Waiting for container to be ready..."
            sleep 10
            
            # Health check
            echo "🏥 Running health check..."
            if curl -f http://localhost/health; then
              echo "✅ Deployment successful!"
            else
              echo "❌ Health check failed!"
              docker-compose logs --tail=50
              exit 1
            fi
            
            # Cleanup old images
            echo "🧹 Cleaning up old images..."
            docker image prune -f

      - name: Notify Success
        if: success()
        run: echo "✅ Deployment completed successfully!"

      - name: Notify Failure
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          exit 1
